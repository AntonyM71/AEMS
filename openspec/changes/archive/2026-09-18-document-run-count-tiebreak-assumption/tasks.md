# Tasks

## 1. Backfill tie-break assumption

- [x] 1.1 Add the run-count-encodes-feature-type requirement to the `scoring` capability
- [x] 1.2 Validate the spec delta (`openspec validate document-run-count-tiebreak-assumption --strict`)
