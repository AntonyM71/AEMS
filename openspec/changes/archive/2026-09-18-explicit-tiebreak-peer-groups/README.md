# explicit-tiebreak-peer-groups

Tie-break reasons name every athlete still tied going into the deciding criterion, not just the adjacent rival, so a judge can show the full breakdown if a placement is challenged. Fixes #434.
