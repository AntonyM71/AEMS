# Tasks

Group 1 lands and passes **before** any code changes, capturing today's behaviour. Groups 2 to
5 implement, groups 6 and 7 hold the bulk of the new coverage, and group 8 holds only what a
running stack can show.

## 1. Baseline the happy path, before changing anything

Nothing today asserts that a judge can submit moves and then submit more. Every existing test
submits once per key: `websocket.spec.ts` and `promotePhase.spec.ts` post one score each, and
`multi-worker.spec.ts` posts one empty score per test. `Scribe.test.tsx` does assert that a
single tap reaches the server, but never a second tap. Write these first, on unchanged code,
so the rest of the work has a baseline that fails loudly if replacement semantics regress.

- [x] 1.1 Add an e2e test that submits a move for one heat/athlete/run/judge, reads it back, submits a longer list for the same key, and reads back again; verify each read returns exactly the list last submitted, and that the second submission replaced the first rather than appending to it. Written as `e2e/tests/scoreSubmission.spec.ts` rather than inside `websocket.spec.ts`, since it drives no browser and `setupTestData` already provides the seeding; a second case covers clearing every move
- [x] 1.2 Add a `Scribe.test.tsx` case that taps two moves in succession; verify a submission fires for each and the second carries both moves
- [ ] 1.3 Run both and confirm they pass against unchanged code; commit them separately so the baseline is visible in history. **Blocked on a running stack**: `Scribe.test.tsx` passes (5/5), but the e2e file has only been type-checked and collected by Playwright, never executed — this environment has no Postgres, backend or frontend, and no Docker access

## 2. Run status uniqueness

- [ ] 2.1 Add a unique constraint on `RunStatus` over (heat_id, phase_id, athlete_id, run_number) in `Server/db/models.py`; verify `alembic revision --autogenerate` emits it
- [ ] 2.2 Make that migration resolve existing duplicates first — where duplicates disagree keep the row with `locked` set, and log what it removes; verify `alembic upgrade head` succeeds against a database seeded with a deliberate duplicate
- [ ] 2.3 Make the downgrade drop the constraint; verify `alembic downgrade -1 && alembic upgrade head` round-trips
- [ ] 2.4 Replace the query-then-insert in `copy_message_to_db` with an `ON CONFLICT DO UPDATE` upsert on that key; verify the existing run-status tests still pass

## 3. Submission ordering

- [ ] 3.1 Add a `RunUpdate` model keyed (heat_id, athlete_id, phase_id, run_number, judge_id) with a `request_id` UUID column and a unique constraint on the key; verify the autogenerated migration creates both
- [ ] 3.2 Add an optional `request_id` to `AddUpdateScoredMovesRequest` in `Server/app/scoring/scoring_logic.py`; verify it round-trips through the endpoint both present and absent
- [ ] 3.3 Validate the identifier's UUID version on ingest, rejecting anything but v7 with `422`
- [ ] 3.4 Put the upsert compare-and-set from design.md at the top of `_persist_athlete_score`, returning early when it reports no win, and treat a missing identifier as newest
- [ ] 3.5 Raise a distinct exception for a stale submission and map it to `409` in `update_athlete_score`, skipping the broadcast on that path

## 4. Locked-run rejection

- [ ] 4.1 Map `UpdatingLockedRunError` to `409` and leave other exceptions on `500`, keeping the locked check ahead of the compare-and-set

## 5. Client-side identifier

- [ ] 5.1 Move `uuid` in `Webapp/package.json` past `^9.0.0` to a version exposing `v7` with an explicit `msecs`; verify `npm run tsc` passes against the new types
- [ ] 5.2 Add a helper that stores `serverDate - Date.now()` from a response `Date` header and returns the corrected time, falling back to the device clock when the header is missing or unparseable
- [ ] 5.3 Mint the identifier from the corrected clock in `submitScores` and send it in the body
- [ ] 5.4 Run `buildApi.sh` from the repo root; verify `Common/openapi.json` and `aemsApi.ts` carry the new field
- [ ] 5.5 Run `npm run precommit` in `Webapp/`; verify type-check, lint and formatting pass

## 6. Server rejection logic unit tests

In `Server/app/scoring/tests/test_customScoringEndpoints.py` against the mocked session,
asserting the status, the persistence calls and the broadcast in each case.

- [ ] 6.1 Identifier validation: accept v7; reject v1, v4 and a malformed string with `422`; accept a submission omitting the identifier
- [ ] 6.2 Stale submission: with the compare-and-set reporting no win, verify `409`, that neither the delete nor `bulk_save_objects` runs, and that nothing is emitted on `/current_scores`
- [ ] 6.3 Winning submission: with the compare-and-set reporting a win, verify the delete and both `bulk_save_objects` calls carry the submitted moves and bonuses, followed by one `/current_scores` emit
- [ ] 6.4 Missing identifier: verify the submission persists and the watermark is written
- [ ] 6.5 Locked run: verify `409`, no persistence, no broadcast, and that a locked run rejects whatever the identifier says
- [ ] 6.6 Unrelated failure: verify an exception from the session still yields `500`, so the new `409` paths have not swallowed real errors
- [ ] 6.7 Run status upsert: verify a second message for one key updates rather than inserts, carrying both `locked` and `did_not_start` through

## 7. Client-side unit tests

- [ ] 7.1 Server-clock offset: cover a device clock ahead, behind and correct, a missing `Date` header, and a malformed one; verify the corrected time tracks the server, and that a missing or unparseable header falls back to the device clock rather than throwing
- [ ] 7.2 Identifier minting: verify `submitScores` sends a v7 identifier drawn from the corrected clock, and that two successive edits produce increasing identifiers
- [ ] 7.3 Rejection handling: with MSW answering `409`, verify the scribe screen keeps showing the local state and stays usable

## 8. End-to-end coverage

Only what a running stack can show. Both tests are deterministic, so they prove the SQL
semantics without depending on timing.

- [ ] 8.1 Submit two scores for one heat/athlete/run/judge in sequence, newer identifier first, through Playwright's request context; verify the second is answered `409` and reading the score back returns the newer submission. This is the test that proves the conditional upsert really rejects. Extend `e2e/tests/websocket.spec.ts`, which already seeds a competition, fetches an available move and maps its direction
- [ ] 8.2 Send two run-status messages for one run; verify reading back yields one status holding the second message's values, proving the constraint and upsert hold against the real schema
- [ ] 8.3 Verify the three existing tests that post scores without an identifier still pass — `websocket.spec.ts` (a move reaching the head judge), `promotePhase.spec.ts` (scores driving promotion) and `multi-worker.spec.ts` (repeated empty posts to one key). They are the regression net for the rollout claim that a submission with no identifier is treated as newest, and `multi-worker` requires that it win every time rather than once
- [ ] 8.4 Run `uv run python -m pytest` and `uv run ruff check .` in `Server/`, and `npm test` in `Webapp/`; verify all pass

Deliberately omitted: tests that fire genuinely concurrent submissions and assert on a winner.
Their outcome turns on connection timing, so they would pass without proving anything and fail
without a bug. Row locking is Postgres behaviour rather than ours; the conditional upsert is
what this change relies on, and 8.1 covers it.

## 9. Decision record

- [x] 9.1 Write `docs/decisions/ADR010-order-judge-score-submissions-with-a-server-stamped-identifier.md` covering the rejected alternatives: move-id ordering, per-element merge, compare-and-swap, debounce with retry suppression, an advisory lock, a device-clock window, and the existing run status record. Delivered during planning
- [ ] 9.2 If the implementation departs from the ADR's decision, update its Decision and Consequences to match what shipped; verify no rejected-alternative rationale has leaked into the spec delta
