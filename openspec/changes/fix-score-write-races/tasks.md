# Tasks

## 1. Run status uniqueness

- [ ] 1.1 Add a unique constraint on `RunStatus` over (heat_id, phase_id, athlete_id, run_number) in `Server/db/models.py`; verify `alembic revision --autogenerate` emits it
- [ ] 1.2 Make that migration resolve existing duplicates first — where duplicates disagree keep the row with `locked` set, and log what it removes; verify `alembic upgrade head` succeeds against a database seeded with a deliberate duplicate
- [ ] 1.3 Make the downgrade drop the constraint; verify `alembic downgrade -1 && alembic upgrade head` round-trips
- [ ] 1.4 Replace the query-then-insert in `copy_message_to_db` with an `ON CONFLICT DO UPDATE` upsert on that key; verify the run-status tests in `Server/app/scoring/tests/` still pass

## 2. Submission ordering

- [ ] 2.1 Add a `RunUpdate` model keyed (heat_id, athlete_id, phase_id, run_number, judge_id) with a `request_id` UUID column and a unique constraint on the key; verify the autogenerated migration creates both
- [ ] 2.2 Add an optional `request_id` to `AddUpdateScoredMovesRequest` in `Server/app/scoring/scoring_logic.py`; verify it round-trips through the endpoint both present and absent
- [ ] 2.3 Reject a `request_id` whose UUID version is not 7 with `422`; verify a test refuses a v4 identifier and accepts a v7 one
- [ ] 2.4 Put the upsert compare-and-set from design.md at the top of `_persist_athlete_score` and return early on an empty result; verify a test proves the delete-and-insert never runs on the stale path
- [ ] 2.5 Treat a missing `request_id` as newest, applying the submission and writing the watermark; verify a test applies an identifier-less submission after one that carried an identifier
- [ ] 2.6 Raise a distinct exception for a stale submission and map it to `409` in `update_athlete_score`, skipping the broadcast; verify a test asserts the status and that no `/current_scores` emit fires

## 3. Locked-run rejection

- [ ] 3.1 Map `UpdatingLockedRunError` to `409` and leave other exceptions on `500`; verify a test gets `409` for a locked run and `500` for an unrelated failure

## 4. Client-side identifier

- [ ] 4.1 Move `uuid` in `Webapp/package.json` past `^9.0.0` to a version exposing `v7` with an explicit `msecs`; verify `npm run tsc` passes against the new types
- [ ] 4.2 Add a helper that stores `serverDate - Date.now()` from a response `Date` header and returns the corrected time; verify a unit test tracks the server value with the device clock set wrong in both directions
- [ ] 4.3 Mint the identifier from the corrected clock in `submitScores` and send it in the body; verify a test finds a v7 identifier in the submitted body and increasing identifiers across two successive edits
- [ ] 4.4 Run `buildApi.sh` from the repo root; verify `Common/openapi.json` and `aemsApi.ts` carry the new field
- [ ] 4.5 Run `npm run precommit` in `Webapp/`; verify type-check, lint and formatting pass

## 5. End-to-end coverage

- [ ] 5.1 Submit two scores for one heat/athlete/run/judge out of identifier order through Playwright's request context; verify the stored score matches the newer one and the older gets `409`
- [ ] 5.2 Fire overlapping submissions for one key concurrently; verify the stored moves match exactly one submission and hold no rows from the other
- [ ] 5.3 Submit concurrently for two judges on one run; verify both scores persist independently
- [ ] 5.4 Send two concurrent run-status messages for one run; verify exactly one status row remains
- [ ] 5.5 Run `uv run python -m pytest` and `uv run ruff check .` in `Server/`; verify both pass

## 6. Decision record

- [x] 6.1 Write `docs/decisions/ADR010-order-judge-score-submissions-with-a-server-stamped-identifier.md` covering the rejected alternatives: move-id ordering, per-element merge, compare-and-swap, debounce with retry suppression, an advisory lock, and a device-clock window. Delivered during planning
- [ ] 6.2 If the implementation departs from the ADR's decision, update its Decision and Consequences to match what shipped; verify no rejected-alternative rationale has leaked into the spec delta
